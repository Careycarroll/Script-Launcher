[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
timeout = 10
addopts = -v --tb=short -m "not pptx"
markers =
    pptx: requires PowerPoint + Automation permission (slow, opt-in)
